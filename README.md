# 🎙️ Podcast BR

スマホ完結型のポッドキャスト運営ツール。収録した **.WAV をアップロードするだけ**で、整音・MP3変換から、タイトル・説明文・ショーノート・チャプター・SNS告知文の生成までを自動で行い、Spotify for Creators への投稿を最短化します。

## 仕組み

```
[スマホブラウザ PWA]
  ① .WAV を選択
  ② ブラウザ内で 整音 → MP3変換 → 音量正規化(サーバー不要・Web Worker)
  ③ Gemini API(無料枠)で文字起こし+メタデータ一括生成
  ④ ワンタップコピー + MP3 ダウンロード + 履歴保存
  → Spotify for Creators アプリに貼り付けて投稿(約30秒)
```

- **完全無料で運用可能**: 静的サイト(GitHub Pages)+ Gemini API 無料枠。サーバー費用ゼロ。
- **プライバシー**: 音声処理はすべて端末内で完結。音声は Google の Gemini API 以外へ送信されません。API キー・履歴は端末内にのみ保存されます。
- **Spotify for Creators に投稿 API がないため**、最後の投稿操作のみ Creators アプリで行う半自動方式です。

## 生成される素材

| 項目 | 内容 |
| --- | --- |
| タイトル案 | 切り口を変えた3案。タップで採用タイトルを記録 |
| 説明文 | 冒頭で核心を伝え、聴きどころを列挙。文字数は設定で調整可 |
| 説明欄まとめて貼り付け | 説明文+チャプター+ハッシュタグを1つに連結(Creators に一度で貼れる) |
| ショーノート | 話題の流れを追える Markdown 箇条書き |
| チャプター | `MM:SS` 形式の話題の切り替わり |
| ハッシュタグ / 検索キーワード | 日本語圏で実際に使われる語 |
| SNS告知文 | X(140字以内)/ Instagram / メール・ブログ用の3種 |

## 音声処理

すべて端末内(Web Worker)で実行されます。設定画面で個別に ON/OFF できます。

| 処理 | 内容 | 既定 |
| --- | --- | --- |
| 低域カット | 80Hz 以下のゴロつき(空調音・机の振動)を除去 | ON |
| ノイズ低減 | 下向きエクスパンダで定常ノイズを最大 -18dB 抑制。声の区間はほぼ無加工 | ON |
| 無音カット | 1秒を超える沈黙を詰める(前後の余韻は残す) | OFF |
| 音量正規化 | RMS -18dBFS を目標に調整、ピークは -1dBFS 以内に制限 | 常時 |

## セットアップ

### 1. デプロイ(初回のみ)

1. GitHub リポジトリの **Settings → Pages → Source** を「**GitHub Actions**」に設定
2. `main` ブランチに push すると自動でビルド&デプロイされます
3. 発行された URL をスマホで開き、「ホーム画面に追加」で PWA 化

### 2. API キー(初回のみ・無料)

1. [Google AI Studio](https://aistudio.google.com/apikey) で API キーを無料発行(クレジットカード不要)
2. アプリの「設定」タブにキーを貼り付け
3. 「番組の背景情報」に番組名・テーマ・リスナー層を書くと生成品質が大きく上がります
4. トーン(カジュアル〜プロフェッショナル)、タイトルの方向性、禁止語、定型フッターも設定可能

### 3. 毎回の運用

1. 「作成」タブで収録した .WAV を選択
2. 完成したら MP3 をダウンロード、各項目をコピー
3. Spotify for Creators アプリで新規エピソード → MP3 選択 → 貼り付け → 公開

生成結果は「履歴」タブに自動保存されます(音声は直近5件、本文は無期限)。

## 開発

```bash
npm install
npm run dev                    # 開発サーバー
npm run build                  # 本番ビルド
npx tsx scripts/smoke-test.ts  # 音声処理・プロンプト生成の検証
```

スモークテストは合成音源を使い、ハイパスが低域だけを落とすか、ノイズ低減が声を残すか、無音カットが話者区間を壊さないかを dB 値で検証します。

## 実装メモ

- **Gemini Files API は `uploadType=media` を使用**。公式が案内する resumable 方式はアップロード先URLが `X-Goog-Upload-URL` ヘッダーで返るが、このヘッダーは `Access-Control-Expose-Headers` に含まれないためブラウザからは読み取れない(実 API で確認済み)。
- 音声処理は「ハイパス → ノイズ低減 → 無音カット → 正規化」の順。正規化を最後にするのは、無音やノイズを除いた実音声を基準に音量を決めるため。
- 履歴は IndexedDB(MP3 Blob をそのまま保持するため localStorage は不可)。

## ロードマップ

- **v1**: WAV→MP3 変換、音量正規化、タイトル・説明文・ショーノート・チャプター・ハッシュタグ生成
- **v2(現在)**: 整音(低域カット・ノイズ低減・無音カット)、履歴保存、トーン/文字数/禁止語などのプロンプト設定、SNS告知文生成、キャンセル対応
- **v3**: 話者分離表示、過去回を踏まえたシリーズ一貫性、RSS 自前配信による完全自動投稿(Creators からの移行)

## 制約・既知の注意点

- Gemini 無料枠にはレート制限があります(429 エラー時は1分ほど待って再試行)
- 長時間(60分超)のエピソードはスマホでの処理に数分かかることがあります
- 対応 WAV 形式: 16/24/32bit 整数 PCM・32bit float、モノラル/ステレオ
