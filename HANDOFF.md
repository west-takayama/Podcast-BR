# 引き継ぎ

このファイルは、このプロジェクトを**初めて触る人(または AI)**が最初に読むものです。
`README.md` は「何がどう作ってあるか」の説明書で、こちらは「**どう進めるか**」の申し送りです。

---

## 1. これは何か

**スマホだけでポッドキャストの投稿準備を終わらせる道具**です。収録した音声を1つ選ぶと、
整音して MP3 にし、タイトル・説明文・チャプター・ハッシュタグ・切り抜き候補まで作ります。
できた文章を Spotify for Creators にコピペすれば投稿できます。

使っているのは**ひとりの運営者**（ポッドキャスト番組の制作者）です。毎回この道具で作業します。
仮説上のユーザーではありません。**実際に困っていることが、そのまま要望として来ます。**

公開先: https://west-takayama.github.io/Podcast-BR/ （`main` への push で自動デプロイ）

---

## 2. 壊してはいけない約束

ここを破る変更は、たとえ便利でも入れてはいけません。**何度も確認された前提**です。

| 約束 | なぜ |
| --- | --- |
| **サーバーを持たない** | 静的サイト1枚。運用費も管理もゼロにするため |
| **無料で使える** | Gemini の無料枠だけで完結する。有料 API を前提にしない |
| **写真と動画は端末から出ない** | 送るのは 32kbps モノラルに落とした音声だけ |
| **API キーは localStorage だけ** | 控え(バックアップ)にも**絶対に入れない**。控えはメールや Drive に載せて運ぶもの |
| **音声処理はすべて端末内** | Web Worker でストリーミング処理。60分の回でもメモリは約100MB |

---

## 3. 最初にやること

```bash
npm ci
npm test          # 部品の検証。5本、427項目。終了コードで見ること
npm run build
npm run dev       # 開発サーバー
```

### `npm test` は終了コードで見る

`grep "ALL OK"` で見ると**先に通った分だけ**が見えて、後ろが落ちていても気づけません。
実際にそれで、ID3 の検証が落ちているのに「ALL OK」と報告した失敗があります。

```bash
npm test; echo "exit=$?"
```

`id3-check` は Python の `mutagen` を使います。入っていないと止まります。

```bash
pip install mutagen
```

### 画面ごしの検証

```bash
npm i -D playwright && npx playwright install chromium   # 1回だけ
npm run build
npm run e2e        # 9本、10〜20分
npm run e2e -- patrol-ui   # 1つだけ
```

詳しくは `e2e/README.md`。**ここが本体です。** 見つかった不具合のほとんどは、
単体テストでは絶対に捕まらないものでした（「画面には出ているのに、配信するファイルだけが空」など）。

---

## 4. 進め方

### 直す前に、まず再現させる

このプロジェクトで見つけた不具合は、**全部そうやって見つけました**。推測で直すと、
直ったかどうかが分かりません。

1. 失敗を再現するテストを書く（落ちることを確認する）
2. 直す
3. 通ることを確認する
4. **修正を一時的に外して、そのテストが落ちることを確認する**（`git stash` → `npm run build`）

4 をやらないと、「もともと通っていた」のか「直したから通った」のか区別できません。

### 測っていないことは書かない

「速くなりました」「品質が上がりました」は、数字が無ければ書かないでください。
このプロジェクトでは実測値を README に残す習慣になっています。

過去に、スクリーンショットの縦ピクセル数（端末解像度3倍）を画面の長さとして報告した
失敗があります。1回だけの測定を代表値として報告した失敗もあります。**単位と回数を確かめてください。**

### 分からないことは分からないと書く

- 再生数のデータは**手元にありません**（Spotify に API が無い）。
  「この回が伸びた」「バズる」と書かせないこと
- お題の提案には「伸びるかどうかは分かりません」と添えてあります
- 検索で裏取りできなかったときは「調べた」と言わない

### 1つの PR で1つのこと

コミットメッセージと PR 本文には、**なぜそう直したか**を書いてください。
「何を直したか」はコードを見れば分かります。分からないのは理由のほうです。

---

## 5. 地図

```
src/
  App.tsx              画面全体の流れ。変換 → 生成 → 結果、タブの切り替え
  components/          画面の部品（結果・履歴・設定・ショート・次の回）
  lib/
    gemini.ts          Gemini とのやり取り。★ 落とし穴が多い（後述）
    encoder.worker.ts  整音と MP3 化。ファイルを2〜3回読む
    prompt.ts          生成の注文文。番組ごとの個性はここに集約
    audio/             整音の部品（フィルタ・ラウドネス・リミッター・レベラー）
    video/clip.ts      縦型ショートの描画
    history.ts         IndexedDB（回・中断復帰・写真）
    backup.ts          控えの書き出しと読み込み
    errorLog.ts        うまくいかなかった記録
scripts/               部品の検証（npm test）
e2e/                   画面ごしの検証（npm run e2e）
```

---

## 6. 落とし穴（同じ失敗を繰り返さないために）

ここに書いてあるものは**全部、実際に起きた**ものです。コードが不自然に見えたら、
まずここを読んでください。理由があります。

### Gemini の応答

| 落とし穴 | どうなっていたか |
| --- | --- |
| **本文は `parts[0]` とは限らない** | 検索を使うと複数の部品に分かれて返る。先頭だけ読んでいて**全機能が落ちた** |
| **考えている途中の部品が混ざる** | `thought: true` の部品は答えではない |
| **JSON は「最初の `{` から最後の `}`」では切れない** | 前置きに `{`、後ろに `}` があると読み違える。括弧の対応を文字列の中まで見て数える |
| **途中で切れることがある** | 全部捨てず、値が完成している位置まで戻して閉じる。題名と説明文は先頭にあるので残っていることが多い |
| **混雑(503)はモデルごと** | 同じモデルを待つより、空いている別のモデルへ移るほうが速い（33秒 → 4秒） |
| **429 には2種類ある** | 1分の上限は60秒で戻る（待つ）。1日の上限は戻らない（待たせない）。混ぜると、戻らない枠を1分おきに押させることになる |

### その他

| 落とし穴 | どうなっていたか |
| --- | --- |
| **`ImageBitmap` の持ち主を2人にしない** | 写真の部品が閉じた後にアプリ側が使い、**MP3 のタグが丸ごと消えていた** |
| **絵の失敗で全部を巻き込まない** | カバーが描けないだけで題名もチャプターも落ちていた。別々の `try` にする |
| **`createdAt` は「最後に触った時刻」ではない** | 控えの新旧判定が一度も働いていなかった |
| **`savedAt` は「中身が変わった時刻」にする** | 保存した時刻にすると、開くたびに新しくなり、端末を替えた直後に控えが負ける |
| **`OfflineAudioContext` でトゥルーピークは測れない** | 補間が直線。窓付き sinc が要る |
| **試験版モデルを既定にしない** | 無料枠の割り当てが細く、混んでいなくても 503 を返す。だから `isPreviewModel` は -100000 |
| **MP3 はリミッターの上限を超える** | 実測で 0.12〜0.38 dB のオーバーシュート。だから天井は -2.0 dBFS |

---

## 7. まだ分かっていないこと

正直に書いておきます。**確かめていないことを「確かめた」と引き継がないでください。**

- **エラーの内訳が分からない。** 「エラーが多い」という報告に対し、混雑なのか上限なのか
  結果が読めないのかを切り分けられていません。設定画面の「うまくいかなかった記録」を
  利用者からもらうのが次の一手です
- **Google 検索での裏取りが、利用者の鍵で動くか未確認。** 出典が出れば動いています
- **iPhone がショートを MP4 で書き出すか WebM に落ちるか未確認**
- **書き起こしと字幕の品質**は測っていません。AI の出力そのもので、こちらで測れる部分が少ない
- **実機でのオフライン動作**（機内モード）は未確認。Service Worker のテストは通っています

---

## 8. 引き継ぎ時点の状態

- `npm test` 終了コード 0（5本・427項目）
- `npm run e2e` 9本すべて通過
- `npx tsc --noEmit` clean
- 最新版はデプロイ済み

### 直近で入れたもの

- 返しきれなかったら項目を9個→5個に減らして自動で再挑戦
- うまくいかなかった記録（設定画面・直近30件・種類別）
- 起動の高速化（4G 相当で 1.42秒 → 約1.05秒）
- 応答の読み取りを頑丈に（12通りの形で検証）

### CI について

`main` への push で `npm test` → `npm run build` → GitHub Pages へ公開、という順です。
**部品の検証が落ちれば公開されません。**

**画面ごしの検証(`npm run e2e`)は CI に入っていません。** ブラウザの用意に時間がかかり、
1回 10〜20分かかるためです。画面まわりを触ったときは、push の前に手元で通してください。
